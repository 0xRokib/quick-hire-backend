// src/modules/applications/application.model.js — Mongoose schema & model for Application
// TODO: Define applicationSchema with fields: job (ObjectId ref), name, email, resumeLink, coverNote, status
// TODO: Add compound unique index on { job, email }
// TODO: Export Application model
